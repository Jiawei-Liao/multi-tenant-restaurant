# Base URL for the API server (leave empty to use Vite's proxy in dev)
VITE_API_BASE_URL=

# The domain suffix appended to tenant subdomains on the customer site
# With nip.io:   .192-168-10-10.nip.io
VITE_TENANT_DOMAIN_SUFFIX=.domain.com
